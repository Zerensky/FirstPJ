Задание 1. Создайте программу на Python или Java, которая принимает два списка чисел и выполняет следующие действия:
a. Рассчитывает среднее значение каждого списка.
b. Сравнивает эти средние значения и выводит соответствующее сообщение:
- ""Первый список имеет большее среднее значение"", если среднее значение первого списка больше.
- ""Второй список имеет большее среднее значение"", если среднее значение второго списка больше.
- ""Средние значения равны"", если средние значения списков равны.


Важно:
Приложение должно быть написано в соответствии с принципами объектно-ориентированного программирования.
Используйте Pytest (для Python) или JUnit (для Java) для написания тестов, которые проверяют правильность работы программы. Тесты должны учитывать различные сценарии использования вашего приложения.
Используйте pylint (для Python) или Checkstyle (для Java) для проверки качества кода.
Сгенерируйте отчет о покрытии кода тестами. Ваша цель - достичь минимум 90% покрытия.

К сожалению у меня не получилось выполнить пункты с проверкой качества кода и отчётом о покрытии,
(внутренняя ошибка windows с виртуальным окружением) но я сделал всё остальное.

В нашем случае, тесты покрывают несколько основных сценариев использования программы, 
которые охватывают различные аспекты её функциональности. Вот какие сценарии покрываются тестами:

Пустые списки: Этот тест (test_empty_lists) проверяет поведение программы при передаче двух пустых списков. 
Программа должна корректно определить, что средние значения равны.

Первый список имеет большее среднее значение: Тест test_first_list_greater проверяет сценарий, 
когда среднее значение первого списка больше среднего значения второго списка. Это помогает убедиться, 
что программа нормально определяет, что первый список имеет большее среднее значение (аж голова заболела пока писал).

Второй список имеет большее среднее значение: Тест test_second_list_greater проверяет обратный сценарий, 
когда среднее значение второго списка больше среднего значения первого списка. Это заверяет нас в том, 
что программа нормально определяет, что второй список имеет большее среднее значение.

Средние значения равны: Тест test_equal_averages проверяет случай, 
когда средние значения обоих списков равны. Этот сценарий важен для проверки того, 
что программа правильно обрабатывает равенство средних значений.

Тест с отрицательными числами: test_with_negative_numbers включает отрицательные числа в списках, 
чтобы убедиться, что программа корректно обрабатывает такие случаи и снова определяет равенство средних значений.

Почему выбраны именно эти сценарии?
Эти сценарии были выбраны для обеспечения полного покрытия основной функциональности программы:

Равенство средних значений: Этот сценарий является базовым случаем, который должен быть корректно обработан программой.

Сравнение списков с разными значениями: Тесты test_first_list_greater и test_second_list_greater проверяют различные ситуации,
 когда один список имеет большее среднее значение по сравнению с другим. Это позволяет убедиться, 
 что программа правильно определяет относительное положение списков.

Специфический случай с отрицательными числами: Использование отрицательных чисел помогает проверить, 
что программа правильно работает с разнообразными типами данных и не зависит от знака чисел.

Эти тесты вместе обеспечивают достаточное покрытие кода, 
чтобы убедиться в его правильной работоспособности на основных сценариях использования.